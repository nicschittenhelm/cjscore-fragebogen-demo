import { IsInViewportDirective } from './is-in-viewport.directive';

describe('IsInViewportDirective', () => {
  it('should create an instance', () => {
    const directive = new IsInViewportDirective();
    expect(directive).toBeTruthy();
  });
});
